export default function SettingsPage() {
  return (
    <div className="page">
      <div className="page-head">
        <div>
          <h1>Settings</h1>
          <p className="page-sub">Defaults that apply across workflows.</p>
        </div>
      </div>
      <div className="empty">
        <p>Coming soon.</p>
        <p className="card-sub">
          Default model, API key, theme — all of it will live here.
        </p>
      </div>
    </div>
  );
}
